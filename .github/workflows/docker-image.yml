name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker-composes up
    - name: Up DB data
      run: cat dump.sql | docker exec -i db psql -U postgres
